cmake_minimum_required(VERSION 2.8.3)
project(vehicle_model)
find_package(catkin REQUIRED)

catkin_package(
)

install(PROGRAMS unzip.sh DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION})

include_directories(
)

